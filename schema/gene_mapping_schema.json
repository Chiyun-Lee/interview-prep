{
  "type": "array",
  "description": "A list of mappings from UniProt IDs to Ensembl gene IDs and gene symbols. Not sure why there are 64,964 entries.",
  "items": {
    "type": "object",
    "properties": {
      "uniprot_id": {
        "type": "string",
        "description": "UniProt accession number (6 characters)."
      },
      "ensembl_gene_id": {
        "type": "string",
        "description": "Ensembl gene ID (ENSG...)."
      },
      "gene_symbol": {
        "type": "string",
        "description": "HGNC gene symbol."
      }
    },
    "required": ["uniprot_id", "ensembl_gene_id", "gene_symbol"]
  }
}